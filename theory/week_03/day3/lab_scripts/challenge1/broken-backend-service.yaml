# 문제 1: DNS 해결 실패 - 잘못된 서비스 이름

apiVersion: v1
kind: Service
metadata:
  name: wrong-backend-service
  namespace: day3-challenge
spec:
  selector:
    app: backend
  ports:
  - port: 3000
    targetPort: 80
